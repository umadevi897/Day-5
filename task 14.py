strings = "Generator-Function : with A generator-function is defined like a normal function, but whenever it needs to generate a vulue. it does with the yield keyword rather than return. if the body of a defcontains yield, the function automatically becomes a generator function."

print(strings)

sample = strings.replace("is", "are").replace("with","that")

print(sample)
